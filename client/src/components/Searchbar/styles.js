import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  searchBarContainer: {
    backgroundColor: "white",
    display: "flex",
    borderRadius: "5px",
    padding: "2px 5px",
    marginTop: "200px",
  },
}));
